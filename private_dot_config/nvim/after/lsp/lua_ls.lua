return {
  settings = {
    Lua = {
      runtime = {
        version = 'LuaJIT',
      },
      --- this should be handled by lazydev
      -- diagnostics = {
      --   globals = { 'vim', 'require' }
      -- }
    }
  }
}
